os:
    - linux
language: java
jdk:
    - oraclejdk8
cache:
    directories:
        - $HOME/.m2
install:
    - java -version
script:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start
    - mvn clean install
